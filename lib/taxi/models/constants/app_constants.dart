// app_constants.dart
class AppConstants {
  // UI Constants
  static const double navigationBarAltura = 60.0;
  static const double buttonEspacamento = -40.0;
  static const double buttonHorizontalPadding = 36.0;

  // Animation Constants
  static const Duration navigationAnimationDuration = Duration(milliseconds: 300);
  static const Duration buttonProcessingDelay = Duration(milliseconds: 300);

  // Sizes
  static const double iconSize = 30.0;
  static const double buttonHeight = 56.0;
  static const double loaderSize = 20.0;
  static const double loaderStrokeWidth = 2.0;
}